#include <bits/stdc++.h>
using namespace std;

int add(int a, int b) {
    if(a<0) exit(1);
    if(b<0) exit(1);
    return a + b;
}

int main(){
    cout<<'HEllo Wold!'<<endl;
    return 0;
}